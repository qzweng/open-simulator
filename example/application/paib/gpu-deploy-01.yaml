apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gpu-deploy-01
  name: gpu-deploy-01
  namespace: paib-gpu
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gpu-deploy-01
  template:
    metadata:
      labels:
        app: gpu-deploy-01
    spec:
      containers:
      - image: tensorflow:1.13
        name: main
        resources:
          limits:
            alibabacloud.com/gpu-mem: 0Mi
            cpu: 8
            memory: 17408Mi
          requests:
            alibabacloud.com/gpu-mem: 0Mi
            cpu: 8
            memory: 17408Mi
      hostNetwork: true
      tolerations:
      - effect: NoSchedule
        key: node-role.kubernetes.io/master
        operator: Exists
